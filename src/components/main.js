import React from "react";
import { motion } from "framer-motion";
import HomeContent from "./home/homeContent";

function Home(props) {
  return (
    <React.Fragment>
      <HomeContent />
    </React.Fragment>
  );
}

export default Home;
