import React from "react";
import { motion } from "framer-motion";
import HomeContent from "./homeContent";

function Home(props) {
  return (
    <React.Fragment>
      <HomeContent />
    </React.Fragment>
  );
}

export default Home;
